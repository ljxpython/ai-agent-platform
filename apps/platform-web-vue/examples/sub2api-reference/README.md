# sub2api Reference Snapshot

这份目录是 `platform-web-vue` 的 UI 资源库参考快照。

用途：

- 为 `Resources` 模板库提供稳定可提交的参考源码
- 保留 `sub2api` 的页面、组件和工程骨架，供开发者查阅和借鉴

来源：

- 原始参考仓：`apps/platform-web-sub2api-base/frontend/src`

边界：

- 这里只是参考快照，不是正式业务代码
- `platform-web-vue` 的 `eslint` 不会扫描这里
- 如需更新参考内容，优先从上游参考仓重新同步快照，而不是在这里长期手工演化
