# 销售报表示例

sales.csv 是实际输入，report.py 是可运行的 Python 标准库程序。
当前脚本故意保留一个教学缺陷：累计单价时漏乘 quantity。
正确金额为 43.50；请定位、最小修改并添加一个可运行的检查。

只读分析时不要执行或修改。需要修复时，先说明计划，通过审批后运行：

```bash
python report.py
```

可继续扩展按商品汇总、输入校验和 CSV 导出；只有用户提出时才实现。
