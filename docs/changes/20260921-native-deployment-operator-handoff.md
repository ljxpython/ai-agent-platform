# 原生开发部署手册收口与运维交接

日期：2026-09-21。仓库级文档改动，用户已同意补齐文档、再次上传并交由运维部署。
本轮不安装服务器软件、不操作服务器数据库、不启动应用、不提交 Git，也不直接向运维发送消息。

## 改动

- `docs/quickstart/deployment-guide.md`：通用账号对齐 `.env.example`，个人交接另行映射；
  将管理入口、已有对象判断、新建角色/库、SCRAM、HBA 首匹配、reload/回退和认证反向测试整理为连续步骤。
- `docs/quickstart/env-matrix.md`、`docs/local-deployment-contract.yaml`：同步通用模板与私有环境覆盖规则。
- `docs/quickstart/operator-handoff.md`：补充运维材料清单、部署顺序、异常处理和验收回执。
- `README.md`、`docs/README.md`、`docs/FEATURES.md`、数据库规范与 AI 部署入口：统一阅读入口和边界。
- 私有运维包：源码快照和最新文档、4 份应用配置及业务建库凭据、实际机器参数和操作说明。
  不包含根目录发布凭据、原始 env 备份、开发机 `.pgpass` 或数据库备份；这些仍保留在原私有交接/备份位置。

## 验证

- 直接从部署手册提取 6.4 节检查代码，在当前本地 PostgreSQL 实际执行：两个账号各完成正确/缺失/错误密码检查，共 6 项 PASS。
- 测试仅新建连接执行 SELECT 1，隔离所有 PG 环境变量和密码文件；网络错误不能冒充认证拒绝。
- 文档相对链接、YAML 契约和 39 段 shell 示例语法检查通过；10 份公开/私有说明未包含实际私密值。
- `python3 scripts/check_docs.py`、本轮 `git diff --check` 通过。
- 新机安装、迁移与真实模型聊天仍由运维执行，不把本机认证测试当作目标服务器部署成功。

## 上传状态

此前 SSH banner 超时；2026-09-21 重试连接成功，新版运维包已完成上传。
远端目录：`/root/ai-agent-platform-handoff/20260920-225507-operator/`。
归档 SHA-256、逐文件 MANIFEST.sha256、私有目录 700 / 文件 600 检查全部通过。
运维以该目录的 `README-运维先读.md` 为入口，实际软件安装、数据库初始化和应用启动尚待运维执行。

## 运维交付原则

以私有 README 的分支、基线提交、源码快照和账号映射部署；空库初始化 admin，创建新项目和模型连接。
回执逐项报告认证反向验证、三条迁移链、四进程、真实聊天和重启复查，不回传密码、Key 或业务数据。
