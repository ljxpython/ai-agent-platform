# P2 研究与交互基础：阶段执行包

> 本文件是 P2 实施时的唯一执行入口。先看需求清单了解做什么、交付标准和当前状态，再按专题链接查实现细节。需求清单与专题任务一起更新；已有代码不等于验收完成。前端暂不实施，需要配合时提前告知。

## 阶段目标

模式、检索、来源、队列、完整表单和专属工作区可用

## 需求清单

本阶段让 Agent 能切换有效执行模式、检索资料并给出可追溯来源，支持完整澄清和运行中的补充消息。

| 需求点 | 要交付什么／怎样算完成 | 对应专题 | 当前状态 |
|---|---|---|---|
| 模式真正生效 | Flash 控制思考、Standard 基础执行、Pro 开启规划；按模型能力应用预算和推理参数，不能只返回模式名称 | 02/C02 | ✅ 四模式组合验证；真实 Pro 研究链路最终 success；不支持思考控制的模型保留默认并记录原因 |
| 普通委派与模式隔离 | Ultra 依赖普通子 Agent 与双重委派保护；不同模式并发不互相污染，未就绪的模式不能宣称可用 | 02/C02；05 | ✅ 普通受限子图、模式隔离已验证；独立 child 控制后置 |
| 可信运行参数升级 | execution_mode 纳入 Context v2；Platform 签发、Runtime 验证使用一致哈希，恢复不允许改模式或模型 | 02/C03；08/W02 | ✅ 双端哈希、模式不可变、真实 Pro 中断／Worker 重启／恢复已验证 |
| 搜索与网页抓取 | 返回真实检索和正文；控制超时、重定向、SSRF 与响应大小；缺凭据明确不可用 | 03/T-A | ✅ Tavily 真实搜索／提取通过；Runtime 只连接固定供应商，DNS／重定向由供应商处理 |
| MCP 接入与授权 | 复用现有工具装配，处理工具名冲突、权限、会话关闭、断线和 scope；探测不发起外部调用 | 03/T-B | ✅ 普通只读 MCP 真实协议调用通过；服务器会话计数未单独断言，写工具暂不开放 |
| 来源与结果证据 | 来源与调用、线程绑定，报告引用可追溯；验证真实产物、哈希、大结果外置及失败状态 | 03/T-E | ✅ 来源只读／原子写／哈希／截断；真实平台引用报告已发布并通过下载核验 |
| 工具能力目录 | 服务端统一声明可用工具及权限，能力展示与真实授权一致，不维护两套列表 | 03/T-F；08/W01 | ✅ capabilities.py 单一映射；MCP 由服务端 allowed_tools 声明，执行仍校验绑定与权限 |
| 完整七类澄清字段 | 支持 text、textarea、number、select、multi_select、checkbox、date；校验必填、多字段、日期、有限数值和选项 | 02/C04-c | ✅ 双端向量和真实平台七字段提问／回答通过；前端本轮后置 |
| 提问错误与恢复保护 | 网关回答预校验、未知版本拒绝、畸形请求修正或明确失败；约束修正预算，多中断按 ID 恢复 | 02/C04；08/W05 | ✅ 双端校验、畸形请求明确失败；七字段真实部署恢复通过 |
| 运行中补充消息 | 接入已有消息队列，明确 queued 与 consumed；验证断线、重复发送及恢复，不把排队当已处理 | 08/W05 | ✅ 重复入队202、唯一 consumed 回执及报告补充标记通过；共享授权缺陷已修复 |
| 上下文和循环保护 | 组合官方摘要、大结果处理、模型／工具调用限额，验证摘要后仍保留任务与控制语义 | 06/M01 | ✅ test_context.py 2 passed；摘要保留控制状态，重建图后累计限额生效 |
| 装配快照与排障 | 记录实际模型、模式、prompt/skills/policy 哈希；日志不记录正文或凭据 | 02/C05 | 已接入 agent.py trusted_metadata；外部观测平台导出受本机网络影响，未验收 |

本阶段用固定工具场景验证研究能力；23 个公共 Skills 的正式迁移从 P4 开始。PDF、Excel 与媒体格式按 P4/P5 所需补齐，不纳入本阶段批量实现。

## 必读上下文

- 总纲：[README.md](../README.md)
- 主专题：[02-agent-composition-and-modes.md](../02-agent-composition-and-modes.md)
- 任务切片：`02/C02—C05；03 研究／MCP／证据；06/M01；08/F2`

## 本阶段任务

### 2026-09-14 本轮实时进度

| 完成项 | 代码路径（仓库相对路径） | 测试证据 |
|---|---|---|
| [x] 七类字段与双端答案校验 | `apps/runtime-service/src/runtime_service/services/dearflow_agent/schemas.py`；`apps/platform-api/src/platform_api/modules/runtime_gateway/application/clarification.py` | `apps/runtime-service/tests/services/dearflow_agent/test_p2_contracts.py`：1 passed，覆盖全部字段、布尔混数值、非有限值、非法日期、重复／未知选项，并用同一组输入运行两服务 |
| [x] Context v1/v2 哈希一致性 | Runtime `runtime/contracts.py`、`runtime/resolver.py`；Platform 网关 `application/service.py` | 上述同组测试验证双端哈希一致；`tests/runtime` 与 `test_modes.py` 合计 52 passed。模式装配和部署恢复门禁尚未全部完成 |
| [x] 研究工具／来源／模式组合 | `apps/runtime-service/src/runtime_service/services/dearflow_agent/tools/search.py`、`agent.py`、`modes.py` | `test_research.py` 9 passed，含真实 Tavily 搜索与提取、来源只读、危险 URL、四模式规划并发；另一个 Ultra 测试 1 passed |
| [x] 普通 MCP 真实协议接入 | `apps/runtime-service/src/runtime_service/services/dearflow_agent/tools/mcp.py` | `test_mcp_tools.py` 1 passed；独立 HTTP MCP 服务调用、绑定隔离、冲突、写工具拒绝、断连 |
| [x] 已有队列持久化／崩溃恢复回归 | `apps/runtime-service/tests/services/test_message_inbox_postgres.py` | 20 项基础通过；2 项进程崩溃测试调整冷启动窗口后通过；1 项旧真实模型探针未启用，Dear 平台链路另验 |
| [x] 跨进程审批恢复及研究最新回归 | `apps/runtime-service/tests/services/dearflow_agent/test_restart.py`、`test_research.py` | 12 passed，429.53s；真实 PostgreSQL 与 Tavily，修复供应商可选空正文，恢复进程冷导入超时证据见 06 |
| [x] 恢复模式不可变／队列授权回归 | `apps/platform-api/tests/test_run_requests.py`；`apps/runtime-service/src/runtime_service/middlewares/message_queue.py` | 网关 20 tests OK（22.861s），明确验证 Pro 恢复保持及改 Ultra 拒绝；队列授权 3 passed、1 个旧真实模型探针 skipped（44.42s） |
| [x] 真实平台研究功能闭环 | `apps/runtime-service/tests/services/dearflow_agent/test_platform.py` | project `946ff99e-768a-42a3-adc2-33bbe24685f3`，thread `733665d1-d94f-4eaa-b33a-fab99ca3b9c0`，最终 Run `342bb241-8d25-4938-9bff-934535b43445` 为 success；报告哈希／来源／补充标记／唯一 consumed 回执核验通过。原客户端超时与后续终态分别记录于 06 |
| [x] 成功线程可重复只读复核 | `test_platform.py:test_completed_research_delivery`（同上完整目录） | 1 passed、1 deselected，49.83s；精确检查 consumed、唯一消息和终态，不用文本包含匹配，不冒称重新执行整条链路 |
| [x] 装配哈希与追踪字段保护 | `apps/runtime-service/src/runtime_service/services/dearflow_agent/agent.py`、`modes.py` | test_modes 2 passed（29.53s）；重建图澄清恢复1 passed（94.37s）；外部追踪导出另列未验收 |
| [ ] 重复部署稳定性 | 现有 GraphHarbor Worker／Redis 运行环境 | 新线程 `dcd2ea14-3f93-4c73-81e4-6fa7da15243f` 重跑出现 Redis 控制流超时，Run 触发300秒限制；不能把一次功能成功写成稳定性通过 |

- [x] C02 后端组合：实现模式对应的规划、思考与委派行为、预算解析和并发隔离；真实模型门禁单列于上表。
- [x] C04-c 后端：完成七类字段、多字段校验、畸形请求明确失败；不新增自动修正循环，前端后置。
- [x] C03 后端：Context v2 双端签发／验证／恢复。
- [x] 03 本轮范围：研究、普通只读 MCP、证据链能力；其他供应商与媒体后置。
- [x] 只创建本阶段实际需要的目录和文件。
- [x] 实施后写入项目 `implementation/`，并回填专题任务与总纲游标。

## 前置条件

- [x] 前一阶段已完成或明确记录 partial／blocked。
- [x] 本阶段涉及的契约生产方与消费方已在 08 章登记。

## 验收

- [x] 使用确定性测试、独立 PostgreSQL／MCP／Tavily 和真实平台研究链路验证；详细结果与限制见 06。
- [x] 未实现能力标记 deferred／partial，不用模拟结果勾选完成。

## 状态

后端功能已实现，真实研究／恢复／队列／发布闭环通过；整体验收仍为 **partial**：本机重复运行出现 Redis／Worker 超时，外部观测导出未验收，前端按用户要求后置。未把这些风险勾成完成，也未开展独立 child 控制、23 Skills 或媒体能力。

下一次接续先看 [06 的最终部署排障与复核命令](../implementation/06-p2-research-and-interaction.md)，处理上述稳定性门禁；不要重新开发已完成的 P2 能力。

前端接入清单已细化在 [frontend-handoff.md 的 P2 前端实施清单](../frontend-handoff.md#p2-前端实施清单待接入验收不代表代码全部缺失)：能力与四模式、七字段、研究来源、补充消息、报告下载及错误恢复。均先核对现有 Dear 副本再补齐，本次只更新文档，不代表 F2 完成。P3 的具体范围与施工顺序见 [下一阶段执行包](P3-子%20Agent%20展示与观测.md)。
