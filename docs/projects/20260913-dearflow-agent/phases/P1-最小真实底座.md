# P1 最小真实底座：阶段执行包

> 本文件是 P1 实施时的唯一执行入口。先看需求清单了解做什么、交付标准和当前状态，再按专题链接查实现细节。需求清单与专题任务一起更新；已有代码不等于验收完成。前端暂不实施，需要配合时提前告知。

## 阶段目标

Dear Agent 路由跑通 TXT→处理→提问／审批→下载→刷新恢复

## 需求清单

本阶段要让一个真实 TXT 任务经过授权、文件处理、提问或审批，最终交付可读取的文件。

**进度核查：2026-09-14。** 当前授权的后端范围为 **done（已完成）**；完整平台文件／审批／Worker 重启验收通过，见 [05 收口记录](../implementation/05-p0-p1-deployment-verification.md)。浏览器端验收按用户要求本轮后置，不宣称前后端整体完成。

| 需求点 | 要交付什么／怎样算完成 | 对应专题 | 当前状态 |
|---|---|---|---|
| DearFlowAgent 注册和装配 | DeepAgents 组合根、薄 graph 导出、运行注册与技能资源打包；探测阶段不产生外部 I/O | 01/A04；02/C01 | ✅ 专属构图、探测无外部初始化和 wheel 资源检查通过 |
| 受信运行边界 | 校验 tenant/project/thread/graph 和 Context 哈希；拒绝伪造身份及未授权工具 | 01；02/C01 | ✅ 专属 scope/Context 篡改、Standard 禁止 task/Todo 与签名文件隔离测试通过 |
| 会话文件隔离 | 上传、工作目录、输出目录按受信会话隔离；通用文件链路不绑定 Showcase | 04/W01—W03；08/W03 | ✅ Runtime JWT 文件上传／下载、tenant/graph/thread 隔离和 symlink 负例通过 |
| TXT 输入与最小技能 | 读取用户 TXT，加载只读烟测 Skill，生成工作文件；烟测不计入 23 项迁移 | 04；07 公共准备 | ✅ 真实模型读取技能／TXT，经 Docker 处理并发布正确产物通过 |
| 受限容器执行 | 审批后执行，资源／网络／挂载限制生效；超时与取消后清理资源 | 04/W02 | ✅ Dear 专属审批执行、只读挂载、超时、输出上限及取消无残留副作用通过；并发容量留 P7 |
| 基础提问与审批 | text/select 使用官方 interrupt/resume；写入、执行、发布走 HITL；混批不提前执行副作用 | 02/C04-b；08/W05 | ✅ 专属两字段提问、approve/edit/reject、混批、网关 422 零提交及幂等测试通过 |
| 真实产物发布和下载 | 发布实际文件，记录大小、类型和哈希，经授权接口下载；不得伪造文件成功 | 04/W03、W05；08/W03 | ✅ 发布前审批、发布后快照、JWT 下载与 hash／缺文件错误测试通过 |
| 恢复同一任务 | 中断后续接同一线程，恢复原配置与文件引用；区分图重建和真实服务重启 | 02；04；08/W05 | ✅ 图重建、PostgreSQL 两进程与真实部署 Worker 重启均通过，原 checkpoint 保留并交付正确文件 |
| 最小能力与目标发现 | 服务端返回可用图、Standard 模式和文件能力，网关授权后可查询 | 08/W01、W07 | ✅ 正式项目 Agent 注册、线程创建、能力查询及授权／脱敏测试通过 |

前端历史进度保留在下方既有记录；本轮不修改或重新验收前端。后端各项通过不等于页面端到端验收通过。

## 必读上下文

- 总纲：[README.md](../README.md)
- 主专题：[01-architecture-and-boundaries.md](../01-architecture-and-boundaries.md)
- 任务切片：`01/A04；04/W01—W03、W05；08/F1、W01、W03、W05、W07；02/C01、C04`

## 本阶段任务

### 已完成的代码与记录

- [x] 创建 `services/dearflow_agent/agent.py`，通过 `create_deep_agent` 装配 Runtime 策略和调用限制。
- [x] 创建 `graphs/dearflow_agent.py` 薄导出，并在 `langgraph.json` 注册。
- [x] 在组合根与 WorkspaceMiddleware 中加入受信 scope、线程及 Context 哈希校验代码。
- [x] 创建 Dear workspace backend 和公共执行器，增加容器镜像定义；执行器已接入 Showcase。
- [x] 创建只读烟测 Skill 及脚本，在 `pyproject.toml` 声明包资源；未计为上游 Skill 迁移。
- [x] 创建 text/select 澄清 schema、官方 interrupt 工具、混批 guard 及工具审批配置。
- [x] 创建实际 TXT 产物发布／哈希读取代码；Runtime 文件接口及 Platform 文件代理加入 outputs 路径支持。
- [x] 能力声明移入 Dear Agent，公共入口兼容导出，对外只声明 Standard；见 [归属调整记录](../implementation/03-capabilities-location.md)。
- [x] 08/F1 前端专属模块、路由及澄清卡片已有实现；据 [前端实施记录](../implementation/20260914-f1-frontend.md)，类型检查及 164 个单测通过。本次未修改、未重跑前端测试，不据此认定真实后端闭环通过。
- [x] 后端实现及环境限制已有 [实施记录](../implementation/20260914-p0-p1.md)。

### 功能验收进度

- [x] 验证正式构图、探测无外部资源初始化及 wheel 中技能和脚本资源。
- [x] 增加 Dear graph 授权、工具权限及 Runtime 签名文件隔离回归。
- [x] 补齐网关澄清答案预校验，验证 text/select、approve/edit/reject、混批与重复恢复。
- [x] 真实模型 TXT → 技能 → Docker → 审批 → 产物测试通过；JWT 上传／下载及内容哈希另有接口测试。
- [x] 验证容器只读挂载、超时、输出上限、取消无延迟写入。
- [x] 验证图重建及两个真实进程之间的 PostgreSQL checkpoint 恢复，文件结果正确。
- [x] 实现 Platform 线程能力查询并补授权／路由／脱敏测试。
- [x] 更新 01／08／10 与总纲、FEATURES 和逐文件实施记录。
- [x] 完整部署的基础文本 Run 已复测通过：项目／Agent 注册、能力查询、真实模型调用、最终状态读取；此前 502 为历史失败，最新复查见 04 实施记录。
- [x] 完整部署文件同任务链路及 Worker 重启通过：真实上传 → 澄清 → 重启并恢复 → execute 审批 → 发布审批 → 下载及 SHA-256 校验；1 passed in 374.63s。

## 前置条件

- [x] 前一阶段已完成或明确记录 partial／blocked。
- [x] 本阶段涉及的契约生产方与消费方已在 08 章登记。

## 验收

- [x] 本次复跑共享 scope／文件引用及 Showcase 执行链路：52 passed、3 skipped。
- [x] 完成 Dear Agent 专属确定性测试、集成测试及真实平台 HTTP 端到端链路；浏览器 E2E 本轮不验。
- [x] 区分代码落地、历史测试结果与当前未验证项；未将前端单测视为后端闭环验证。

## 验证证据与限制

本次在 `apps/runtime-service/` 执行：

```bash
.venv/bin/python -m pytest -q tests/test_scoped_and_refs.py tests/services/showcase_demo/test_documents.py
```

结果：**5 passed**。覆盖共享 scope 哈希、图片引用及已有文档工具行为，不覆盖 Dear graph 的执行、恢复或文件 HTTP 全链路。

上述 5 项为早期核查记录。最新专属测试、真实模型、Docker、PostgreSQL 两进程恢复、网关测试及精确代码定位见 [04 实施记录](../implementation/04-p0-p1-foundation-closeout.md)；此前“无专属测试／Docker 不可用”已不再是当前状态。

## 状态

已完成（done，当前授权后端范围）：21 项专属测试、真实模型任务、完整部署基础文本及完整文件／审批／Worker 重启链路通过。代码路径、复跑命令及项目／线程证据见 [04 修复记录](../implementation/04-p0-p1-foundation-closeout.md) 和 [05 部署收口](../implementation/05-p0-p1-deployment-verification.md)。前端本轮不改、不重验；后续须单独完成浏览器入口验收。
