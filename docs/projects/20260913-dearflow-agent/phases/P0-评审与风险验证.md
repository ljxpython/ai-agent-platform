# P0 评审与风险验证：阶段执行包

> 本文件是 P0 实施时的唯一执行入口。先看需求清单了解做什么、交付标准和当前状态，再按专题链接查实现细节。需求清单与专题任务一起更新；已有代码不等于验收完成。前端暂不实施，需要配合时提前告知。

## 阶段目标

锁版本、架构决策、Spike 证据、契约和部署拓扑

## 需求清单

本阶段回答“架构是否可行、风险如何验证”，不以创建目录代替风险验收。

**进度核查：2026-09-14。** 当前授权的后端范围为 **done（已完成）**；真实部署及 MCP 风险证据见 [05 收口记录](../implementation/05-p0-p1-deployment-verification.md)。前端按用户要求不在本轮验收。

| 需求点 | 要交付什么／怎样算完成 | 对应专题 | 当前状态 |
|---|---|---|---|
| 确认迁移范围与人工评审 | 固定 DeepAgents 底座、Web 产品范围、23 个 Skills 逐项迁移；浏览器操作不做 | 01/A01—A03 | ✅ 已完成：用户明确批准规划及 P0/P1 实施 |
| 冻结依赖和上游来源 | 记录 Python、LangChain、LangGraph、DeepAgents、运行引擎版本，以及上游修订、许可证和采用资源 | 01；07 公共准备 | ✅ 本轮基线已完成：上游 HEAD／MIT／参考文件哈希、uv.lock 哈希和安装版本已记录；尚未迁移 23 Skills |
| 确认代码归属 | 后端业务放 services/dearflow_agent 子目录，graph 只做导出；公共组件保持公共归属，不依赖 deerflow-harness | 01 | ✅ 已按边界实现并通过构图／共享回归，代码定位见 04 |
| 验证官方暂停与恢复 | 实测提问、工具审批、连续 interrupt ID、混合工具批次和重复 resume；记录锁版本结果 | 02/C04-a；08/C05 | ✅ 专属 graph／网关回归及完整部署提问、审批、Worker 重启恢复通过；未来引擎升级须复跑 |
| 冻结服务间契约 | 明确图标识、能力、Context、文件引用、事件的生产方、消费方、版本与升级策略 | 08/C01—C05；02/C03 | ✅ P1 最小 C01—C05 已冻结并通过平台 HTTP 联调；P2 扩展另验，见 05 契约表 |
| 验证执行环境和文件持久性 | 明确容器镜像、权限、资源限额、共享文件卷、worker 与重启后的文件归属 | 04 | ✅ 本机 API／Worker／Docker 持久目录和重启恢复通过；不声明跨机多副本可用 |
| 确认记忆存储边界 | 记录 Store 命名空间、隔离和并发写能力；完整记忆功能留 P6 | 06/S3、M02 | ✅ 风险识别完成：真实 PostgresStore 持久／隔离／删除通过；公开 aput 无 CAS，P6 必须补原子修订保护 |
| 验证长任务可恢复性 | 用任务服务验证提交、回执丢失、取消和重启；冻结供应商的幂等边界 | 09/B02 | ✅ 官方 MCP 可控任务服务故障试验通过；正式持久业务及真实媒体供应商仍属 P5 |

独立子 Run、单子任务取消与独立用量归属按既定决策后置，不作为 P0 必须实现的能力。

## 必读上下文

- 总纲：[README.md](../README.md)
- 主专题：[01-architecture-and-boundaries.md](../01-architecture-and-boundaries.md)
- 任务切片：`01/A01—A03；02 模式风险；04 资源；05 S2；06 S3；08/F0、C01—C05；09/B02`

## 本阶段任务

- [x] 确认迁移范围并取得用户对规划及 P0/P1 的实施授权。
- [x] 在 01 章记录 Python 与框架版本对照、服务边界及目录归属。
- [x] 形成 [P0/P1 实施记录](../implementation/20260914-p0-p1.md)，记录已创建模块与环境限制。
- [x] 补齐上游修订、许可证、参考文件及 uv.lock 哈希，记录采用范围，wheel 烟测资源检查通过。
- [x] 完成 P1 官方 interrupt/HITL/resume 试验，覆盖连续中断 ID、混批与网关重复恢复。
- [x] 冻结 P1 最小 C01—C05 契约，并以真实平台 HTTP 文件／恢复链路验证生产方和消费方。
- [x] 验证本机真实容器、持久工作目录和 PostgreSQL 跨进程 checkpoint 恢复，记录部署路径前提。
- [x] 完成本机 Agent Server API／独立 Worker 与持久目录部署验证，重启 Worker 后恢复原 checkpoint、处理并下载正确文件。
- [x] 补齐 Store 隔离及持久性试验，明确无 CAS 的公开接口边界；完整记忆与并发修订实现留 P6。
- [x] 完成可控 MCP 长任务提交、ACK 丢失、取消及重启试验；真实供应商及持久业务仍须在 P5 媒体迁移前通过。
- [x] 在 01／08／10 与总纲更新本轮当前状态和最新证据入口；历史规划记录不作为当前完成度。

## 前置条件

- [x] 本阶段为首阶段，无前一阶段依赖；实施已获用户授权。
- [x] 08 章已有 C01—C05 登记；P1 后端最小契约冻结及实现证据见 05。

## 验收

- [x] 运行 Showcase、共享 workspace、文件引用回归：52 passed、3 skipped。
- [x] 运行 Dear Agent 专属 graph、签名文件 HTTP、网关 Run／授权／路由契约测试；完整部署边界单列。
- [x] 本文件逐项记录未完成、部分完成与环境限制；不再将全部风险审查勾选完成。

## 核查依据

- [01 版本与架构基线](../01-architecture-and-boundaries.md)：存在版本对照与边界设计；不能据此认定运行试验通过。
- [后端实施记录](../implementation/20260914-p0-p1.md)：记录了代码落地和 Docker 限制，尚无完整 P0 风险试验结果。
- 最新证据：[04 专属底座修复与验收](../implementation/04-p0-p1-foundation-closeout.md)。已新增 Dear 专属测试；此前“无专属测试／Docker 不可用”为历史状态。

## 状态

已完成（done，当前授权后端范围）：本机完整部署、最小契约及 B02 风险试验均已补齐，见 [05 收口记录](../implementation/05-p0-p1-deployment-verification.md)。前端验收本轮后置；生产多机、P5 长任务业务、P6 Store 原子修订不属于已完成声明。
