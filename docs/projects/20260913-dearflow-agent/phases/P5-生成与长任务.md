# P5 生成与长任务：阶段执行包

> 本文件是 P5 实施的唯一执行入口。执行顺序：集中完成本批代码与测试入口，再统一验证；失败仅定向复验。实现记录：[12-P5代码与验证](../implementation/12-p5-media-and-tasks.md)。

## 当前范围（用户于2026-09-15调整）

本批完成 **K12 图片生成／编辑、K13 图像式 PPTX、图片付费回执、PPTX 发布／授权下载**。
用户决定 **K14播客、K15音乐延迟开发；K16异步视频、音视频大文件／Range后续实施（deferred）**。不再索取延期项目的供应商配置，延期项保留在后续任务清单。
视频专属后台轮询、取消、交付和真实验收一并后置。完整长MCP恢复／outbox仅在后续实际需要时实施，本批不预建。
不是用同步图片回执冒充完整长任务底座。前端只交接，页面验收后置。

## 需求、代码与状态

**最新续验（2026-09-15）：** K13三次AI生成→完整PPTX发布下载已通过；K12文生图、单图编辑已成功，新供应商图片域名白名单已补齐，多参考图实际连接异常仍待验。此前503/524是历史请求结果，不能概括为供应商永久不可用。详见[12记录顶部](../implementation/12-p5-media-and-tasks.md)。

| 需求点 | 实际交付／定位 | 后端状态 | 前端交接 | 页面验收 |
|---|---|---|---|---|
| K12 文生图／编辑 | `services/dearflow_agent/tools/media.py:build_media_tools` 复用 `tools/images.py`；1张主图＋最多3张参考图；真实图片校验、引用 | partial：文生图／单图编辑成功；主图＋3参考图连接异常，待验 | 见F5交接 | deferred |
| 图片付费去重 | `services/dearflow_agent/external_task_storage.py`、`migrations/001_external_tasks.sql`：审批后先持久化意图、稳定key、租约、fence、unknown、不盲重试 | done（图片回执范围）：真实PostgreSQL及Run中的unknown／无重购验证通过 | 展示业务status，unknown不显示成功／自动重购 | deferred |
| K13 图片式PPTX | `skills/ppt-generation/scripts/generate.py`：保留上游排版和备注；沙箱组装，1—20页、缺图失败、顺序一致 | done：Docker、上传三图、三次AI生成→完整PPTX发布下载均通过 | 展示图片型说明、页图、PPTX下载 | deferred |
| PPTX交付 | `workspace/media.py`、`workspace/artifact_refs.py`、Platform `runtime_client.py:read_file`：安全ZIP/XML、禁宏／外部关系、哈希引用、MIME白名单 | done：确定性测试＋真实模型发布／平台下载哈希通过 | 既有授权文件下载，无新URL | deferred |
| K14 多角色播客 | 分角色TTS、规范拼接、MP3与文字稿；当前未启用工具／Skill | deferred：用户决定延迟开发 | deferred | deferred |
| K15 音乐生成 | 音乐服务、凭据／配额、付费回执、MP3；当前未启用工具／Skill | deferred：用户决定延迟开发 | deferred | deferred |
| K16 异步视频 | 后续接入视频供应商、Skill、worker、MP4交付 | **deferred：保留需求，后续实施** | deferred | deferred |
| B03—B06完整长任务 | 正式任务worker、双worker接管、outbox、授权续接、真实媒体重启取回；图片回执只是一部分基础 | deferred：仅在K14/K15或实际长MCP需要时恢复；视频部分随K16后续实施 | deferred | deferred |
| 音视频大文件／Range | 大文件、Range、断点传输 | **deferred：保留需求，后续实施** | deferred | deferred |
| B02既有MCP Spike | P0旧证据通过，P4复验超时；不等于真实媒体长任务交付 | partial：历史失败仍保留于11记录，本批不依赖／不宣称修复 | 不适用 | 不适用 |

上表 Runtime 代码相对 `apps/runtime-service/src/runtime_service/`；Platform 代码位于 `apps/platform-api/src/platform_api/adapters/langgraph/`。完整代码路径和测试命令见12记录。

## 本批检查清单

- [x] 读取07/K12—K16、09/B02—B06及现有代码；按用户调整缩减范围。
- [x] K12／K13复制上游资源、许可证、来源哈希，并最小适配执行入口。
- [x] 图片工具、持久回执、PPTX沙箱、发布与下载代码及测试入口集中实现。
- [x] PostgreSQL回执组合测试、共享图片／文件回归和平台适配器测试。
- [x] 真实Docker三页PPTX、尺寸／顺序、缺图验证。
- [x] 新供应商文生图真实成功；编辑返回域名白名单修正，原响应图片下载／校验成功。
- [x] K12单图编辑真实平台链路：编辑／保存／下载哈希／报告／v3回放通过（1 passed，915.88s）。
- [x] 修复非法图片发布导致Run异常终止：受控错误反馈给模型，文档发布权限保持不变；文件定向3项通过。
- [ ] K12完整验收：文生图及单图编辑成功；多参考图APIConnectionError，无HTTP状态，仍待验。
- [x] K13上传三图→真实模型组装→PPTX发布与平台下载验证。
- [x] K13三次AI生成均成功→完整PPTX：真实平台下载、3页、哈希、MIME及v3事件回放通过。
- [x] 最终安装包、质量检查和文档进度同步。
- [x] K14/K15及实际长任务标deferred；K16与音视频大文件／Range标deferred，保留后续实施及验收依赖。
- [x] 前端保留交接，页面验收不算后端完成。

## 上下文与后续

- 总纲：[README](../README.md)
- 技能：[07/K12—K16](../07-skills-migration.md)
- 完整长任务设计（本批后置）：[09](../09-background-work-and-scope.md)
- 前端：[frontend-handoff/F5](../frontend-handoff.md)
- 本批具体代码／失败／证据：[12](../implementation/12-p5-media-and-tasks.md)

P4 K03／K05外部限流、K09 dual_axes远端错误、旧MCP探针复验超时仍保留，不由本阶段覆盖。
Goal自动续跑、定时任务、持久批次继续后置。

## 状态

部分完成（partial）。本批后端代码、确定性／数据库／Docker验证已完成；K13完整AI三页链路done；K12文生图、单图编辑成功，多参考图连接异常待验。K14/K15延期；K16和音视频大文件后续实施（deferred）；前端交接done，页面deferred。
