# P0/P1 实施记录

## 已完成

- 新增 DearFlowAgent graph、DeepAgents 组合、运行时能力声明和 Dear workspace。
- 新增受限容器执行、产物发布/读取、文本澄清协议和工具审批链路。
- 文件 HTTP 解析改为签名 scope + graph 能力校验，支持 outputs 产物读取。
- Showcase 执行器改用公共 workspace executor。
- 增加 agent workspace 镜像定义和 graph 注册。

## 未完成/环境限制

- 独立子 Agent 的取消、进度、用量归属按规划后置，P1 复用现有 task 能力。
- 本机 Docker daemon 不可用，真实容器成功路径未验证；相关集成测试保留并如实失败。
- 前端未实现，交接要求见 `frontend-handoff.md`。
